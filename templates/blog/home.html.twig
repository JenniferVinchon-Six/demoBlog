
{% extends "base.html.twig" %}
{# extends permet d'hériter de tout les blocks définit ds le gabarit de base "base.html.twig" #}

{# Le block "title" fait référence au block "title" déclaré ds le fichier "base.html.twig", le rendu apparait dans l'onglet du navigateur #}
{% block title %}
    Blog | Home
{% endblock %}

{# Le block "body" fait référence au block "body" déclaré ds le fichier "base.html.twig" #}
{# Le contenu du bloc 'body' est envoyé dans le bloc 'body' de base.html.twig et l'ensemble est envoyé en réponse sur le navigateur (base + template)  #}
{% block body %}

    <div class="row text-center my-5">
        <h1>Bienvenue sur le blog Symfony 👌</h1>

    {#
        TWIG est un langage de rendu.
        AVANTAGE :
            - simplicité du language : facilités d'écriture et d'affichage, apporte beaucoup de fonctionnalités
            - Absence de PHP au sein de nos affichages, permet d'abstraires les affichages des balises PHP
            - Plus simple pour un intégrateur.

        {{ }} --> double accolades, c'est ce que l'on appel l'interpolation (ex: affichage variable, execution fonction twig)

        {% %} --> commande TWIG (ex: boucle, condition etc...)
    #}

    {# title : variable TWIG qui provient du controller "BlogController" de la méthode "home()" définit ds les paramètres de la méthode de rendu "render()" #}
    <h2>{{ title }}</h2>

    {# age : variable TWIG qui provient du controller "BlogController" de la méthode "home()" définit ds les paramètres de la méthode de rendu "render()" #}
    {#     25      #}
    {% if age > 18 %}
        <p>Tu es majeur !!</p>
    {% else %}
        <p>Tu es mineur !!</p>
    {% endif %}
    </div>
    
{% endblock %}

